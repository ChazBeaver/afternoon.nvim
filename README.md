# afternoon.nvim

`afternoon.nvim` is the Neovim colorscheme for the broader **Afternoon** theme ecosystem.

This repo contains the Neovim theme itself. It is intended to pair with the Omarchy theme repo:

- **Omarchy theme repo:** `omarchy-afternoon-theme`
- **Neovim theme repo:** `afternoon.nvim`

## What this repo is

This repo is focused on Neovim only.

It provides the editor colorscheme, highlight groups, and plugin styling needed for Neovim to match the rest of the Afternoon theme.

## Relationship to Omarchy

`afternoon.nvim` is the editor-side counterpart to `omarchy-afternoon-theme`.

That means:

- `afternoon.nvim` handles Neovim colors and highlights
- `omarchy-afternoon-theme` handles desktop and app theming for Omarchy
- both are meant to share the same palette and visual direction

## Theme pairing

Use these together for a matching setup:

- **Neovim:** `afternoon.nvim`
- **Omarchy:** `omarchy-afternoon-theme`

## Goal

The goal of `afternoon.nvim` is to make Neovim feel like a natural part of the wider Afternoon environment instead of a separate, mismatched editor theme.
